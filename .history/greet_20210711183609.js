const names = ["Khamis", "Jokudu", "Anna", "Loburi"];

const sayHi = (name) => {
  if (name == "Khamis") {
    console.log(`Hello big bro ${name}!`);
  }
};

sayHi(
  names.forEach((name) => {
    console.log(name[(5, 0)]);
  })
);
